import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    success: {
      main: "rgb(1, 135, 68)",
    },
    background: {
      default: "#dfdede6e",
    },
  },
});
